services:
  - type: web
    name: token-counter-mcp
    runtime: python
    buildCommand: pip install -r server/requirements.txt && cd web && npm install && npm run build
    startCommand: python server/main.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.0"
